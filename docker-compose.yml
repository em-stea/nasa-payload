services:
  mongo:
    image: mongo:7
    container_name: nasa-backoffice-mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    command:
      - --storageEngine=wiredTiger
    volumes:
      - data:/data/db

volumes:
  data:
